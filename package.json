{
  "name": "session-registry-plugin",
  "private": true,
  "version": "0.0.0",
  "description": "Session Registry portable agent plugin workspace",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "verify:full-fidelity-publication-contract": "pnpm exec tsc -b --clean packages/core packages/mcp-server && pnpm exec tsc -b --force packages/core packages/mcp-server && pnpm --filter @session-registry/mcp-server run test:stdio"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
